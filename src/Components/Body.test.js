import React from 'react';
import { render } from '@testing-library/react';
import Body from './Body';
import { handleDot } from './Body';

test(`this is an empty test`, () => {

})
/*


test('check the class constructor function'), () {
  const instanceBody = new Body();
  expect(instanceBody).toHaveProperty(state);
}

console.log(instanceBody);

test('check if the dot "." button works', () => {
  console.log(Body.handleDot)
  console.log(handleDot)

  const startState = { answer: '0', storedValue: '0', operation: '', plusMinusPressed: false, decPressed: false, numPressed: false }
  const endState = handleDot()
  expect(endState).toEqual({ answer: '0', storedValue: '0', operation: '', plusMinusPressed: false, decPressed: true, numPressed: true })
});





  const { getByText } = render(<Body />);
  const linkElement = getByText(/calculator/i);
  expect(linkElement).toBeInTheDocument();
});

*/